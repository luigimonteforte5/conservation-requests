spring:
  datasource:
    url: jdbc:h2:mem:conservation-requests;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  docker:
    compose:
      enabled: false

app:
  security:
    jwt-secret: not-a-secret-test-only-jwt-signing-key-0123456789
    admin-username: sa
    admin-password: $2a$12$ObxTqLVMyw2pPx775TKVHe9bf707Vgtrvx5uwuqu.bofVmP7LPLFa
  encryption:
    key: test-encryption-key
